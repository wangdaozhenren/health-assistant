---
name: health-assistant
slug: health-assistant
displayName: 药品健康助手
description: 药品健康比价助手——OTC/保健/器械正规渠道比价，处方药只给指引。当用户想比药品价格、药哪个平台便宜、买药比价、保健品价格、医疗器械比价、京东健康阿里健康美团买药、drug price comparison 时使用。覆盖：医保比价小程序（官方）、京东健康、阿里健康大药房、美团买药。只比同厂家同规格同剂型；药品说明书要点查询；处方药仅提示凭方正规购药，不列购买入口。只做信息查询与比价，不构成诊疗建议，不代下单、不代付款。
summary: "药品健康比价助手——OTC/保健/器械正规渠道比价，处方药只给指引。当用户想比药品价格、药哪个平台便宜、买药比价、保健品…"
version: 1.0.2
license: MIT
tags: 药品比价,买药,京东健康,阿里健康,美团买药,医保比价,OTC,保健品,医疗器械,处方药,drug-comparison,药品健康助手
---

# 药品健康助手

OTC/保健/器械，正规渠道比价；处方药只给就医/凭方购药指引。

## 工作流

1. **解析需求**：提取 `药品/商品名`、`通用名或厂家`、`规格（mg/ml/片数）`、`剂型`（片剂/胶囊/口服液）、可选 `平台清单`。药品名不清晰时先问一次；**先确认是否处方药**（Rx 标识），处方药走第 5 步指引流程。

2. **合规前置判断**：
   - **处方药（Rx）**：不列购买入口、不引导下单。仅提供：①正规购药渠道提示（凭处方在医院/合规互联网医院/实体药店）②说明书要点查询（读 `doubao-answer-with-medical-evidence` 或权威来源）③提示遵医嘱。
   - **OTC/保健品/医疗器械**：进入正常比价流程。

3. **平台路由**：默认医保比价小程序（官方，免登录）→ 京东健康 → 阿里健康大药房 → 美团买药（用户点名平台时以用户为准）。动手前读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")` 操作浏览器。

4. **逐平台采集**：搜索药品名（优先通用名）→ 读前 5~10 条。每条记录至少含：`平台`、`药品`、`厂家`、`规格`、`剂型`、`价格`、`店铺/药房`、`链接`。

5. **登录/验证移交**：登录墙/验证码出现时，**下一步必须是** `interaction.request_action(type="browserControl")`，用户处理后交回再重读。

6. **规格归一（读 `references/health-assistant-knowledge.md`）**：
   - **只比同厂家同规格同剂型**：阿莫西林 0.25g×24 粒 vs ×12 粒不同价，换算单粒价可比；
   - 保健品按含量/粒数归一（如每粒含 XX mg × N 粒）；
   - 医疗器械（血压计/血糖仪等）按型号归一。

7. **报告输出**：Markdown 表 `平台 | 药品 | 厂家/规格 | 价格 | 单粒(单位)价 | 药房 | 链接 | 备注`，结论给出"最低价正规渠道 + 规格口径提醒"。**每个价格必须附可点击的购买链接**。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md`）。链接增强为平台官方合作渠道的优惠链接，**链接可能含推广/返佣属性（受益方为技能运营方），在推荐输出中如实向用户标注返佣/推广属性，并提供不带归因参数的原站直达链接选项**。

8. **盯价提醒**（可选）：用户说"这个药降到 X 提醒我"时写入盯价清单（读 `doubao-cron-scheduler/SKILL.md`）。

## 关键规则

- **低频访问（绝对规则）**：单平台单日 ≤3 次、单药品 ≤2 次/日、两次页面操作间隔 ≥10 秒、验证码立即停采当日不重试。用户催促不构成放宽理由。
- **处方药红线**：不列处方药购买入口、不引导"无方购药"、不做用药推荐替代医嘱。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证走 `interaction.request_action` 移交。
- 不导出 Cookie/登录态；不代下单、不代付款；价格页面实读，禁止编造。

## 边界声明

- 本技能仅提供价格信息查询，**不构成诊疗建议、不替代医嘱**；用药请遵医嘱或咨询执业药师。
- 药品价格以平台实时显示为准；同药品不同药房/平台价差正常，报告标注读取时点。
- 处方药购药请凭处方在正规渠道进行；互联网医院开具处方请选择合规平台。
